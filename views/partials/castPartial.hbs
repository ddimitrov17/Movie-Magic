<div class="cast-card">
    <div class="card-img">
        <img src={{imageUrl}} alt="cast">
    </div>
    <div class="cast-info">
        <h1>Name: {{name}}</h1>
        <h3>Age: {{age}}</h3>
        <h3>Born: {{born}}</h3>
        <h3>Name in movie: {{nameInMovie}}</h3>
    </div>
</div>